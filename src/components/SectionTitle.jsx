const SectionTitle = ({ title }) => {
  return <div className="border-b border-1 py-4 mb-8">
    <h2 className="text-primary font-semibold text-2xl tracking-wide">{title}</h2>
  </div>
}
export default SectionTitle;